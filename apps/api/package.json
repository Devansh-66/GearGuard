{
    "name": "gearguard-api",
    "version": "0.0.0",
    "scripts": {
        "dev": "wrangler dev src/index.ts",
        "db:setup": "wrangler d1 migrations apply gearguard-db --local",
        "deploy": "wrangler deploy --minify src/index.ts"
    },
    "dependencies": {
        "@gearguard/db": "*",
        "drizzle-orm": "^0.45.1",
        "hono": "^4.0.0"
    },
    "devDependencies": {
        "@cloudflare/workers-types": "^4.20240101.0",
        "wrangler": "^3.0.0"
    }
}